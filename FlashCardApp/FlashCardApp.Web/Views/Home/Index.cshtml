@using System.ServiceModel.Security
@using FlashCardApp.Core.Entities
@using FlashCardApp.Web
@model System.Collections.Generic.List<FlashCardApp.Web.Models.FlashCardSet>

@{
    
    ViewBag.Title = "User Sets";
    Layout = "../Shared/_BootstrapLayout.basic.cshtml";
}


<!--todo: session variable to keep loging current -->
    
    @if (!ViewBag.UserIsLoggedIn)
    {
        using (Html.BeginForm("Index", "Home"))
        {
            <div>
                User Name:@Html.TextBox("userName")

                Password:@Html.Password("password")
                <input type="submit" value="submit" />
            </div>
        }

    }
    
    @if (Model != null)
    {
        <div class="page-header">
            Flash Card Sets
        </div>
        foreach (var set in Model)
        {
            <div class="focused">
                @Html.DisplayName(set.SetName)<br/>
                @Html.ActionLink("Display Set", "DisplaySet" , new{setID = set.ID})
                <br/>
                @Html.ActionLink("Study Set" , "StudySet",  new{setID = set.ID})
                
                <br/>
            </div>
        }
    }

